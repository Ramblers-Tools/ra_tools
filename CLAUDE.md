# ra_tools — Instructions for Claude Code

This is the code repo for RA-Tools (com_ra_tools), a Joomla component. It does not include the vault's persistent notes/graph.

Vault notes: `~/vault/ra-tools/`
Knowledge graph: `~/vault/ra-tools/graph/`

Query the graph first for structure/architecture questions rather than re-reading the whole codebase — see `~/vault/CLAUDE.md` for the 3-layer query rule and session commands (`/vault-resume`, `/vault-save`).
